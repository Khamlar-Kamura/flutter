name: maid_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0' # <-- Corrected syntax

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  provider: ^6.1.2
  intl: ^0.19.0            # <-- Corrected version
  cupertino_icons: ^1.0.8
  flutter_map: ^7.0.2      # <-- Corrected version
  latlong2: ^0.9.0
  geolocator: ^12.0.0      # <-- Corrected version

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/

  fonts:
    - family: Phetsarath
      fonts:
        - asset: fonts/Phetsarath-Regular.ttf